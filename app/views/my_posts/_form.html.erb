<%= simple_form_for ([post.status], remote: true) do |f| %>
  <%= f.input :active, label: false, placeholder: "Active" %>
  <%= f.input :done, label: false, placeholder: "Done"%>
  <%= f.button :submit, class: "btn btn-warning" %>
    <% end %>
